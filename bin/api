#!/usr/bin/env node
require("commander")
  .version(require("../package").version)
  .usage("<command> [options]")
  .command("project", "query and set project info")
  .command("interface", "query and set interface info")
  .command("result", "query and set interface info")
  .command("setting", "configuration setting")
  .parse(process.argv);
